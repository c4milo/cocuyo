\* expect: holds
\* TCP, two lookups on one connection, where a query waits behind another's (the stream's rule 9).
CONSTANTS
    Servers = 2
    Slots = 2
    Conns = 1
    PollsMax = 1000
    TimeoutTicks = 2
    UseTcp = TRUE
    PerPort = 0
    Tls = FALSE
    Request = FALSE
    RStream = FALSE
    OpsMax = 4
    FailuresMax = 1
    SentMax = 3
INIT Init
NEXT Next
INVARIANT Clean
CHECK_DEADLOCK FALSE
