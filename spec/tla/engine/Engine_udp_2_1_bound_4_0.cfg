\* expect: holds
\* UDP, two lookups sharing each server's socket, a port replaced every two queries.
CONSTANTS
    Servers = 2
    Slots = 2
    Conns = 1
    PollsMax = 1000
    TimeoutTicks = 2
    UseTcp = FALSE
    PerPort = 2
    Tls = FALSE
    Request = FALSE
    RStream = FALSE
    OpsMax = 4
    FailuresMax = 0
    SentMax = 3
INIT Init
NEXT Next
INVARIANT Clean
CHECK_DEADLOCK FALSE
