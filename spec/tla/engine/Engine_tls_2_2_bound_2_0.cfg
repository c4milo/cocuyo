\* expect: holds
\* TLS, two lookups on a connection for each server (§21, TLS rule 6).
CONSTANTS
    Servers = 2
    Slots = 2
    Conns = 2
    PollsMax = 1000
    TimeoutTicks = 2
    UseTcp = TRUE
    PerPort = 0
    Tls = TRUE
    Request = FALSE
    RStream = FALSE
    OpsMax = 2
    FailuresMax = 0
    SentMax = 3
INIT Init
NEXT Next
INVARIANT Clean
CHECK_DEADLOCK FALSE
