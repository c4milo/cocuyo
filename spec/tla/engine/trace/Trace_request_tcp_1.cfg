\* The engine's walks for the replay over DoH on HTTP/2, one slot and two servers, whose request
\* connections run over TCP: TLC's simulation mode, which `zig build spec` runs.
CONSTANTS
    Servers = 2
    Slots = 1
    Conns = 0
    PollsMax = 1000
    TimeoutTicks = 2
    UseTcp = FALSE
    PerPort = 0
    Tls = FALSE
    Request = TRUE
    RStream = TRUE
    OpsMax = 0
    FailuresMax = 0
    SentMax = 0
INIT TraceInit
NEXT TraceNext
INVARIANT Clean
CHECK_DEADLOCK FALSE
