\* The engine's walks for the replay over TCP, one slot and two connections: TLC's simulation
\* mode, which `zig build spec` runs; `zig build tla` does not read this directory.
CONSTANTS
    Servers = 2
    Slots = 1
    Conns = 2
    PollsMax = 1000
    TimeoutTicks = 2
    UseTcp = TRUE
    PerPort = 0
    Tls = FALSE
    OpsMax = 0
    FailuresMax = 0
    SentMax = 0
INIT TraceInit
NEXT TraceNext
INVARIANT Clean
CHECK_DEADLOCK FALSE
