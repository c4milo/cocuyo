\* expect: holds
\* TLS, one slot and two connections, at two operations and no failure: spec/README.md's first
\* TLS row, whose 51,406 states TLC's count must equal.
CONSTANTS
    Servers = 2
    Slots = 1
    Conns = 2
    PollsMax = 1000
    TimeoutTicks = 2
    UseTcp = TRUE
    PerPort = 0
    Tls = TRUE
    OpsMax = 2
    FailuresMax = 0
    SentMax = 3
INIT Init
NEXT Next
INVARIANT Clean
CHECK_DEADLOCK FALSE
