\* expect: holds
\* DoH over HTTP/2, two lookups and one server: a slot reopened while its old connect is in flight.
CONSTANTS
    Servers = 1
    Slots = 2
    Conns = 0
    PollsMax = 1000
    TimeoutTicks = 2
    UseTcp = FALSE
    PerPort = 0
    Tls = FALSE
    Request = TRUE
    RStream = TRUE
    OpsMax = 3
    FailuresMax = 1
    SentMax = 0
INIT Init
NEXT Next
INVARIANT Clean
CHECK_DEADLOCK FALSE
