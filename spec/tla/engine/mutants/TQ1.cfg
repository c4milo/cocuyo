\* expect: violated
\* module: EngineMutants
\* A lookup that leaves keeps its waiting query queued (docs/mutations.md TQ1). Two lookups on one
\* connection: with one, no query ever waits.
CONSTANTS
    Servers = 2
    Slots = 2
    Conns = 1
    PollsMax = 1000
    TimeoutTicks = 2
    UseTcp = TRUE
    PerPort = 0
    Tls = FALSE
    Request = FALSE
    OpsMax = 4
    FailuresMax = 0
    SentMax = 3
    Release <- ReleaseKeepsQueued
INIT Init
NEXT Next
INVARIANT Clean
CHECK_DEADLOCK FALSE
