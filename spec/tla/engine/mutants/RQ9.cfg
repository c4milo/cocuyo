\* expect: violated
\* module: EngineMutants
\* A connection closes for idleness with requests on it (RQ9, request rule 9). Two servers: time
\* moves while a request is on a connection only when another connection is idle.
CONSTANTS
    Servers = 2
    Slots = 1
    Conns = 0
    PollsMax = 1000
    TimeoutTicks = 2
    UseTcp = FALSE
    PerPort = 0
    Tls = FALSE
    Request = TRUE
    OpsMax = 3
    FailuresMax = 1
    SentMax = 0
    CloseIdleR <- CloseIdleRBusy
INIT Init
NEXT Next
INVARIANT Clean
CHECK_DEADLOCK FALSE
