\* expect: violated
\* module: EngineMutants
\* The connection opened again takes its slot while the loop still holds its records (docs/mutations.md R8d).
CONSTANTS
    Servers = 2
    Slots = 1
    Conns = 2
    PollsMax = 1000
    TimeoutTicks = 2
    UseTcp = TRUE
    PerPort = 0
    Tls = TRUE
    Request = FALSE
    RStream = FALSE
    OpsMax = 2
    FailuresMax = 1
    SentMax = 3
    ConnectAgain <- ConnectAgainBorrowed
INIT Init
NEXT Next
INVARIANT Clean
CHECK_DEADLOCK FALSE
